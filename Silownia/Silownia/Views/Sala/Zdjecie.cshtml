@model Silownia.Models.Sala

@{
    if (ViewBag.Pic == 1)
    { 
        
        if (Model.Zdjecie != null)
        {
            WebImage foto = new WebImage(Model.Zdjecie);

            foto.Resize(600, 600, true, true);
            foto.Write();
        }
    }
}
